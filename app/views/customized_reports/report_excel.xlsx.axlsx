wb = xlsx_package.workbook

if @patients_screened 
  wb.add_worksheet(name: "Screened Patients") do |sheet|
    @patients_screened.each do |patient|
      sheet.add_row [patient.first_name, patient.last_name]
    end
  end
end