<h1 class="main-title"> Employer Enrollments </h1>

<div class="flex justify-end">
    <%= link_to "Add new Employer", new_employer_path, class: "button-primary" %>
</div>

<table class="table">
    <thead>
        <tr>
            <th> Full Name </th>
            <th> Company Name </th>
            <th> Address </th>
            <th> Total Employees </th>
            <th> Total Locations </th>
            <th> Website </th>
        </tr>
    </thead>
    <tbody>
        <% @employers.each do |employer| %>
            <tr>
                <td> <%= employer.first_name %> <%= employer.last_name %> </td>
                <td> <%= employer.company_name %> </td>
                <td> <%= employer.address_1 %> </td>
                <td> <%= employer.total_employees %> </td>
                <td> <%= employer.total_locations %> </td>
                <td> <%= link_to employer.website, employer.website if employer.website %> </td>
            </tr>
        <% end %>
    </tbody>
</table>