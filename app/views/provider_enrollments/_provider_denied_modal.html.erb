<div class="opacity-0 pointer-events-none fixed bottom-0 inset-x-0 px-4 pb-4 sm:inset-0 sm:flex sm:items-center sm:justify-center" data-target="provider-enrollment-new-entry.modal" id="providerDeniedMessageModal">
    <div class="fixed inset-0 transition-opacity">
        <div class="absolute inset-0 bg-gray-500 opacity-75"></div>
    </div>

    <div class="w-10/12 relative bg-white rounded-lg px-4 pt-5 pb-4 overflow-hidden shadow-xl transform transition-all sm:p-6 overflow-scroll" style="max-height: 90vh;">
        <div class="hidden sm:block absolute top-0 right-0 pt-4 pr-4">
            <button data-action="click->provider-enrollment-new-entry#toggleModal" type="button"
                class="text-gray-400 hover:text-gray-500 focus:outline-none focus:text-gray-500 transition ease-in-out duration-150">
                <svg class="h-6 w-6" stroke="currentColor" fill="none" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
            </button>
        </div>
        <div> 
            <p class="label">This message will be sent to the denied Provider: </p>
            <%= ProviderDenialMessage.first.body %>
        </div>
        <div class="mt-5 sm:mt-4 sm:flex sm:flex-row-reverse">
            <span class="flex w-full sm:ml-3 sm:w-auto">
                <button type="button" data-action="click->provider-enrollment-new-entry#toggleModal" class="button-primary">
                    Close
                </button>
            </span>
            <span class="mt-3 flex w-full sm:mt-0 sm:w-auto">
                <%= f.submit "Submit", class: "button-primary", data: {action: "click->provider-enrollment-new-entry#setDeniedStatus"} %>
            </span>
        </div>
    </div>
</div>