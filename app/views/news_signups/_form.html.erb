<%= form_with(model: news_signup, local: true) do |form| %>
  <% if news_signup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_signup.errors.count, "error") %> prohibited this news_signup from being saved:</h2>

      <ul>
        <% news_signup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
