Your <%= @clinic.services.pluck(:name).join(", ") %> Appointment is Confirmed! .

This message is to confirm that <%= @patient.first_name[0] + " " + @patient.middle_initial + " " + @patient.last_name %>,
<%= @patient.patient_family_members.select([:first_name, :middle_initial, :last_name]).map{ |m| "#{m.first_name[0]} #{m.middle_initial} #{m.last_name}" }.join(", ") %>
is scheduled for:

Date: <%= @appointment.appointment_at ? @appointment.appointment_at.strftime("%m-%d-%Y") : "On Waiting List" %>,
Time: <%= @appointment.appointment_at ? @appointment.appointment_at.strftime("%H:%M %p") : "On Waiting List" %>,
Location: <%= @clinic.address %>.

To cancel this appointment, click <%= link_to "here", cancel_appointment_clinic_queued_patients_url(cancel_appointment: @cancel_appointment, clinic_id: @clinic.id) %> to cancel.

We look forward to seeing you!
<%= ENV['PROVIDER_NAME'] || "Provider" %>