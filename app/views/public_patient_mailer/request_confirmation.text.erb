Thank you for registering for <%= @clinic.services.pluck(:name).join(", ") %>.

<%= @patient.full_name %>,
<%= @patient.patient_family_members.select([:first_name, :last_name]).map{ |m| "#{m.first_name} #{m.last_name}" }.join(", ") %>
are scheduled for:

Date: <%= @appointment.appointment_at ? @appointment.appointment_at.strftime("%m-%d-%Y") : "On Waiting List" %>,
Time: <%= @appointment.appointment_at ? @appointment.appointment_at.strftime("%H:%M %p") : "On Waiting List" %>,
Location: <%= @clinic.address %>.

If you cannot, make this appointment, click <%= link_to "here", cancel_appointment_clinic_queued_patients_url(cancel_appointment: @cancel_appointment, clinic_id: @clinic.id) %> to cancel.

Thank you